services:
  sql-injection-login:
    build:
      context: ./labs/sql-injection-login
    container_name: vulnmentor-sqli-lab
    ports:
      - "4010:4010"
    restart: unless-stopped

  api-broken-auth:
    build:
      context: ./labs/api-broken-auth
    container_name: vulnmentor-api-bola-lab
    ports:
      - "4020:4020"
    restart: unless-stopped
